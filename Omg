-- [[ CONFIGURATION ]]
getgenv().AutoFarm = true
getgenv().WeaponType = "Melee" -- Chọn: "Melee" hoặc "Sword"

-- [[ DATA TABLE ]]
-- Bạn có thể thêm các đảo khác vào đây từ file mẫu của bạn
local QuestList = {
    {MinLv = 1, MaxLv = 14, Name = "BanditQuest1", Lv = 1, NPC = CFrame.new(1060, 16, 1547), Mon = "Bandit"},
    {MinLv = 15, MaxLv = 29, Name = "JungleQuest", Lv = 2, NPC = CFrame.new(-1601, 36, 153), Mon = "Gorilla"},
    {MinLv = 30, MaxLv = 39, Name = "BuggyQuest1", Lv = 1, NPC = CFrame.new(-1140, 4, 3827), Mon = "Pirate"}
}

-- 3. HÀM TÌM NHIỆM VỤ PHÙ HỢP
-- Hàm này duyệt qua bảng QuestData để tìm nhiệm vụ hợp với Level hiện tại
local function GetCurrentQuest()
    local MyLevel = LocalPlayer.Data.Level.Value
    for _, Data in ipairs(QuestData) do
        if MyLevel >= Data.MinLv and MyLevel <= Data.MaxLv then
            return Data
        end
    end
    return nil -- Không tìm thấy nhiệm vụ (ví dụ Max level)
end

-- 4. HÀM DỊCH CHUYỂN (TWEEN)
-- Hàm này giúp nhân vật bay đến đích
local function TweenTo(TargetCFrame)
    local Character = LocalPlayer.Character
    if not Character or not Character:FindFirstChild("HumanoidRootPart") then return end
    
    local Root = Character.HumanoidRootPart
    local Distance = (TargetCFrame.Position - Root.Position).Magnitude
    local Speed = 200 -- Tốc độ bay
    
    local TweenInfo = TweenInfo.new(Distance / Speed, Enum.EasingStyle.Linear)
    local Tween = game:GetService("TweenService"):Create(Root, TweenInfo, {CFrame = TargetCFrame})
    Tween:Play()
    
    if not Root:FindFirstChild("BodyVelocity") then
        local BV = Instance.new("BodyVelocity")
        BV.Parent = Root
        BV.MaxForce = Vector3.new(1e9, 1e9, 1e9)
        BV.Velocity = Vector3.zero
    end
end

-- 5. LOGIC CHÍNH (VÒNG LẶP)
task.spawn(function()
    while task.wait() do -- Lặp vô tận (để auto liên tục)
        if getgenv().AutoFarm then
            pcall(function()
                -- Bước A: Lấy thông tin nhiệm vụ cần làm
                local CurrentQuest = GetCurrentQuest()
                if not CurrentQuest then 
                    print("Không tìm thấy nhiệm vụ cho cấp độ này hoặc đã Max Level")
                    return 
                end

                -- Bước B: Kiểm tra xem đã nhận nhiệm vụ chưa
                -- Giao diện nhiệm vụ trong Blox Fruits nằm ở PlayerGui.Main.Quest
                local PlayerGui = LocalPlayer.PlayerGui
                local QuestTitle = PlayerGui.Main.Quest.Container.QuestTitle.Title
                
                -- Điều kiện: Nếu bảng nhiệm vụ ẩn HOẶC tên nhiệm vụ đang làm không đúng với quái cần đánh
                local HasQuest = PlayerGui.Main.Quest.Visible and string.find(QuestTitle.Text, CurrentQuest.MonName)
                
                if not HasQuest then
                    -- TRƯỜNG HỢP: CHƯA CÓ NHIỆM VỤ
                    -- 1. Bay đến NPC nhận nhiệm vụ
                    local DistanceToNPC = (LocalPlayer.Character.HumanoidRootPart.Position - CurrentQuest.NpcPos.Position).Magnitude
                    
                    if DistanceToNPC > 10 then
                        -- Nếu xa NPC quá thì bay lại gần
                        TweenTo(CurrentQuest.NpcPos)
                    else
                        -- Nếu đã đứng gần NPC (khoảng cách <= 10) thì nhận nhiệm vụ
                        -- Remote "StartQuest" nhận 2 tham số: "Tên Nhiệm Vụ", Level nhiệm vụ (1 hoặc 2)
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("StartQuest", CurrentQuest.QuestName, CurrentQuest.QuestLevel)
                        task.wait(1) -- Đợi 1 giây để server xử lý tránh spam
                    end
                else
                    -- TRƯỜNG HỢP: ĐÃ CÓ NHIỆM VỤ -> ĐI ĐÁNH QUÁI
                    -- Ở đây bạn kết hợp với đoạn code Auto Attack mình đã viết cho bạn ở câu trả lời trước
                    
                    -- Logic tìm quái để bay tới (đơn giản hóa)
                    for _, Enemy in pairs(workspace.Enemies:GetChildren()) do
                        if Enemy.Name == CurrentQuest.MonName and Enemy:FindFirstChild("Humanoid") and Enemy.Humanoid.Health > 0 then
                            TweenTo(Enemy.HumanoidRootPart.CFrame * CFrame.new(0, 30, 0)) -- Bay tới trên đầu quái
                            break -- Tìm thấy 1 con thì bay tới, thoát vòng lặp tìm kiếm
                        end
                    end
                end
            end)
        end
    end
end)

-- 2. Hàm tự động cầm vũ khí
local function EquipWeapon()
    local char = LP.Character
    if not char then return end
    if char:FindFirstChildWhichIsA("Tool") then return end -- Đã cầm thì thôi
    
    for _, v in pairs(LP.Backpack:GetChildren()) do
        if v:IsA("Tool") and (v.ToolTip == getgenv().WeaponType or v.Name == getgenv().WeaponType) then
            char.Humanoid:EquipTool(v)
            break
        end
    end
end

                        -- Fast Attack (Logic từ 3 file bạn gửi)
                        RS.Modules.Net["RE/RegisterAttack"]:FireServer(0)
                        RS.Modules.Net["RE/RegisterHit"]:FireServer(Target.PrimaryPart, {{Target, Target.PrimaryPart}})
                    else
                        -- Nếu không thấy quái ở workspace, bay đến điểm spawn quái
                        TweenTo(q.NPC * CFrame.new(0, 100, 0)) -- Bay cao lên để đợi quái spawn
                    end
                end
            end)
        end
    end
end)
